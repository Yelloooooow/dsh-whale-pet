# 鲸鱼娘桌宠（dsh-whale-pet）· 说明

DeepSeek Harness 固定插件：右下角常驻鲸鱼娘，头顶气泡常驻显示 DeepSeek 剩余余额。
已作为 bundle 加入 web profile，**每次启动 Harness 自动加载**，无需手动操作。

## 目录结构（G:\WorkSpace\DSH\dsh-whale-pet\）

```
dsh-whale-pet/
├── plugin/                  # 固定插件源码（安装到 profile 的源）
│   ├── index.js             # Host 端：/__whale-pet/balance 余额查询、/__whale-pet/image 图片读取
│   ├── client.js            # 浏览器端：右下角鲸鱼娘 + 常驻气泡
│   ├── cordis.patch.yml     # 插件补丁层（插入插件行）
│   └── package.json         # 包声明（dsh.bundle.patch + dsh.client.platform）
├── assets/                  # 素材库（鲸鱼娘形象素材、预览图、素材清单）
├── whale_pet.png            # 当前使用的鲸鱼娘形象（PNG）
├── whale_pet_b64.txt        # 形象图 base64（插件 Host 端从这里读取）
└── 启动说明.md              # 本文档
```

## 资产文件路径（插件硬编码）

- 图片 base64：`G:\WorkSpace\DSH\dsh-whale-pet\whale_pet_b64.txt`
- 插件源码里 `IMAGE_B64_FILE` 常量指向它；改形象时替换 `whale_pet.png` 并重新生成 base64 到 `whale_pet_b64.txt`

## 修改插件后如何生效

1. 修改 `plugin\` 下的源码
2. 把改动的文件同步到 `C:\Users\fctz8\.dsh\profiles\web\node_modules\dsh-whale-pet\`（Host 端 index.js）
3. **重启 Harness**（bundle 内容有内容哈希，重启后加载新版）

## 数据接口（Host 端 webServer 路由）

- `GET /__whale-pet/balance` — DeepSeek 余额（curl → node fetch 双通道，API Key 走凭据服务）
- `GET /__whale-pet/image` — 鲸鱼娘形象图 data URI

## 形象署名

形象来源：fornarwhal/deepseek-whale-girl-icon（CC BY-NC-SA 4.0）
角色原型：上善无形 OC「溟月」；DeepSeek 元素二创：ZipZipPipe（GPT Image 2）
